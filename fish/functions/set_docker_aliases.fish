function set_docker_aliases --description 'Set Docker aliases'
	echo 'Setting docker aliases...'

	abbr dcb 'docker-compose build'
	abbr dce 'docker-compose exec'
	abbr dcr 'docker-compose run'
	abbr dcu 'docker-compose up'
end

